export function inc(n, i) {
  /** insert items into a, @i^th index.*/
  return n + i;
}
export function mul(n, m) {
  /** delete l items from a, @i^th index.*/
  return n * m;
}

export function max(n, m) {
  return Math.max(n, m)
}

export function abs(n) {
  return Math.abs(n);
}
